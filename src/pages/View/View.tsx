import React from 'react';
import Model from '../../components/Model';

export const View = ({ title, hide, children }) => {
  return (
    <>
      <Model title="" hide={true} children=""></Model>
    </>
  );
};
